# Slice: 02-variant-chrome

<!-- mxcli-brain -->

Requirements for this slice. Anchors point FORWARD, at what the slice
will build — an anchor that does not resolve yet means not built, not
stale. `mxcli brain plan` counts them against the model.

## Make the board chrome variant-aware: name the jigsaw's REGION instead of BOX in the status line, and drop the difficulty switch and /81 counter on boards where they do not apply.

Anchors: `@Sudoku.SUB_UnitLabel` · id `019009` · 2026-09-04
