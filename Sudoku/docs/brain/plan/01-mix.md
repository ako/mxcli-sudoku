# Slice: 01-mix

<!-- mxcli-brain -->

Requirements for this slice. Anchors point FORWARD, at what the slice
will build — an anchor that does not resolve yet means not built, not
stale. `mxcli brain plan` counts them against the model.

## Deal a Mix that actually requires reading across the overlap, via a solver that alternates between the two grids rather than repairing each independently.

Anchors: `@Sudoku.SUB_SolveMixJoint` · id `f09401` · 2026-09-04
